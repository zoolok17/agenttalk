# 2026-07-31 — the release sequence, and a blocker the lead introduced

> Project logbook. History is **appended, never edited** — a period record whose past gets rewritten
> stops being evidence. Cites message ids, SHAs, PR numbers and task numbers rather than restating them.
> Companion: `docs/ROADMAP.md` §6.0 (v0.80.0's declared contents), `docs/TASKS.md` (what a cited `#NN` means).

## What this day was about

Yesterday measured the runtime's failure modes. Today tried to **ship** them, and the day's shape was set
by two things: the operator authorising the release, and a fleet-wide CI red that the lead had caused and
then misdiagnosed for several cycles.

## The release contents were not what the lead assumed

The lead had been treating "four green items" as the release. Reading `docs/ROADMAP.md` §6.0 instead:
**v0.80.0 declares #114, #115, #112, #117.** #105 and #120 belong to v0.81.0. So two items the lead was
counting were out of scope, and two genuine gaps were invisible until the roadmap was actually read:
#117 was marked "partially implemented", and #115 had **never been gated** — zero CI runs, because
workflows only trigger on PRs to master and no PR existed.

Merged today, in this order, each gated and connector-checked first:

- `1f44ae4` — PR 106, the gitleaks unblocker (not a release item)
- `a51639d` — PR 101, **#112**, the keystone: a watchdog kill that wedged the wrapper it was protecting

## The fleet-wide CI red was the lead's own commit

For several cycles the lead reported "four independent flakes" across four unrelated branches. It was one
blocker, and the lead had introduced it.

Publishing dev-4's 87-A fold (`0ae2e88`) added a `ChildEstablishmentGuardV1.key` schema type — a string
added to satisfy panel blocker B1 — and gitleaks' `generic-api-key` rule matched it. Because
`actions/checkout` runs with `fetch-depth: 0`, **every PR run scans all fetched branches**, so a commit on
a *design* branch reddened `linux/3.12` on every unrelated PR. Master's green predated the push, which is
why the contradiction looked unresolvable.

The discriminating evidence was available early — two unrelated PRs failing on the identical lane with an
identical leak count of 2 — and the lead held it while calling them independent. dev-8 fixed it correctly:
three `.gitleaksignore` entries pinned to commit + file + rule + **line**, with a comment justifying each.
Not a rule disable, not a path glob. False positives, and a broader fix would have been refused.

## The connector channel earned its keep, nine times

The lead added "read the host's automated review findings before you gate" to the shipped lead skill this
morning (`3456650`), after ignoring that channel for an entire day. Today it caught **nine findings across
three PRs, none of which CI could see**. Every one was either confirmed-and-fixed or refuted with code —
zero hand-waved.

The most consequential: **PR 97 was 15/15 green and about to be merged.** The connector posted two
NEW-ON-HEAD P2s. One was a startup claim race — if `supervisor.kill` appeared between the unlocked
precheck and the locked claim, the claim succeeded and the process **kept executor authority** instead of
exiting 3. That defeats #114's entire purpose, and it would have shipped.

One finding was **refuted, not folded**: the connector claimed `_winapi.PeekNamedPipe` returns a 3-tuple,
so a two-variable unpack crashes every turn. With `nsize=0` it returns a 2-tuple — verified empirically by
both the lead and dev-7 independently. "Fixing" it would have introduced the bug it imagined. The channel
is valuable and **not authoritative**; the producer-check rule applies to forwarded findings too.

## One principle, three faces

Three separate defects today were the same mistake — an acknowledgement mistaken for an outcome:

- a **reported kill** is not an exit confirmation (connector on PR 101; dev-7 fixed)
- a **successful cancel submission** is not a cancellation (`gh run cancel` accepted four requests that
  took 30–70 minutes to apply; the lead read the ✓ as done)
- a **polled `None`** is not an exit confirmation (PR 98: `on_exit` emitted `child_exited` with a null
  return code while the process could still be alive)

Reading *state* rather than *messages* was the single most reliable habit of the day, and every place the
lead skipped it produced a wrong report.

## #117's own purpose was nearly defeated

#117 captures wrapper stdout/stderr so a dying wrapper's traceback is not destroyed by construction. The
connector found that `cmd_wrap`'s `finally` restored the raw streams **before** Python emits an uncaught
traceback — so with a full first segment, that traceback bypassed the bounded tee and was omitted from the
rotating tail. The feature would have captured everything except the one line anyone wants. Fixed by
printing inside the except block while the tee is still installed, guarded so a signal-driven intentional
exit gets no spurious dump.

Also fixed there: generation pruning sorted by wall-clock directory name, so a backward clock correction
made stale future-dated generations look permanently newest and the genuinely newest got evicted. Replaced
with a monotonic per-agent launch sequence. Same lesson as the UTC-clock keystone fix — wall clock is not
an ordering key on a machine an operator can adjust.

## Two stale claims, both refuted by workers

- **#56** was dispatched as "the one concrete blocker on Qwen". Already fixed weeks ago:
  `run.py:562-578` injects workspace-scoped `HOME`/`USERPROFILE`/`LOCALAPPDATA`/`APPDATA`, with the
  historical `signing.default_keys_dir()` crash named in the comment. dev-8 refuted it, ran a restricted-env
  `agenttalk reply` returning 0, **made no edits**, and stopped — as instructed. Closed.
- **#117** was "partially implemented" per the roadmap. dev-6 read four commits of diffs and refuted that
  with citations: a 549-line module, bounds an operator override **cannot** defeat (clamped 4 KiB–64 MiB,
  2–32 segments), nonce-authenticated so an ad hoc `wrap` can't be redirected, unresolved generations
  preserved rather than evicted.

A task description is a claim. The producer-check rule the lead imposes on workers applies to the lead's
own backlog, and it was not applied before briefing the operator on either item.

## #115 could not have been gated earlier

The lead flagged "#115 has never been gated" in five consecutive updates, then tried to fix it and found
out why: cherry-picking `bcc3666` onto master conflicts in `tests/test_supervisor.py`, because #115 was
built on `fc8e681` — **#114's work**, which is not on master. The roadmap said exactly this — "complete,
blocked on the item above" — and the lead had read it as a scheduling note. The dependency is mechanical.
The lead stopped rather than inventing a conflict resolution in someone else's test file to hit a deadline.

## Infrastructure fixed today

- **CI concurrency group** (PR 106's sibling work, `2a81750` → merged as part of the sequence): neither
  workflow superseded stale runs, so every push queued an *additional* 13-job matrix. Measured 8 active
  runs with 4 superseded. master is deliberately **exempt** from cancellation — a release-gating run must
  never be killed by a following push.
- **Tag protection** — `v*` tags were completely unprotected (verified: tag rulesets empty, both active
  rulesets branch-only). Now ruleset `20082397`: creation, deletion and non-fast-forward restricted with an
  admin bypass, **tested end-to-end** because the output was ambiguous (`Cannot create ref due to
  creations being restricted` printed *alongside* a successful `[new tag]`).
- **`docs/TASKS.md`** — the lead had cited numbered tasks for weeks while holding the only copy. Published.

## Where the Windows cost actually is

Measured from a real Windows gate artifact: **pytest is 96.2% of the gate.** Four pytest runs at
1084–1271s each; all fourteen other checks total ~30s. Windows legs run 46–55 min against 13–19 for
linux/macos, because this suite spawns PowerShell, creates venvs and worktrees, and writes many temp files.
Dropping macOS would buy nothing; the Windows *version* count is the only real lever, and it is where real
defects surface. #127 (parallelise pytest) is the only change that reduces latency rather than contention.

## Lead errors worth keeping

- **Introduced the fleet-wide CI red** by publishing `0ae2e88`, then called its symptoms "four independent
  flakes" for several cycles while holding the discriminating evidence.
- **Consumed dev-3's #114 disposition without reading it.** Head-limited drains pulled it in behind dev-6's
  messages and it scrolled past. Found only because `phase=idle`/`outcome=success` with no reply in hand
  did not add up. Cost ~90 minutes of the critical path sitting done-but-unread.
- **Rationalised `MANUAL_RESTART/relaunch` on healthy agents six times**, writing "harmless here since
  they're healthy". It was a six-hour retry loop caused by the lead's own restart requests, which are only
  retired on *successful* relaunch (#129).
- **Praised #126 as the best work of the session before reading its CI** — it was 0/13 at the time.
- **Quoted a stale check count** as current after the PR head had moved.
- Every one of these is a *reading* failure, not a reasoning failure, and they clustered as the session
  extended.

## Open at end of this entry

`master = a51639d`, version still `0.79.1`. v0.80.0 needs: PR 107 (#114) and PR 98 (#117) to go green and
merge, then #115 rebases cleanly and gets its first gate, then the version bump — **re-gated on the
post-bump commit**, not the pre-bump SHA — then the tag.

codex-2 is parked after three distinct failures and two identical failed remedies; it needs a clean-slate
reset, not a fourth `request-restart`. It is off the critical path.

## Correction (appended after the fact): the CI concurrency group did NOT merge

The "Infrastructure fixed today" section above says the concurrency group merged "as part of the
sequence". It did not. Verified after compaction: `git grep concurrency origin/master --
.github/workflows/` returns nothing, and **PR 103 is still open**, red on `linux/3.12` because its branch
predates the `.gitleaksignore` fix on master. So on this date every push still queued an additional
13-job matrix; the measured contention in #125 was described as fixed while remaining live.

The error is the same class as the rest of the day's: a *submitted* change read as a *landed* one. The
sibling claims in that section were checked and do hold — tag ruleset `20082397` exists and is active,
and `docs/TASKS.md` is tracked.

Also established after compaction, and worth recording because it governs every merge decision:
**master is protected by rulesets, not the legacy branch-protection API** (which 404s "Branch not
protected" and reads as *unprotected* if trusted). Ruleset 19368439 requires `dev-gate aggregate` and
`codeql (security-extended)` with `strict` unset — so a red gate genuinely cannot be merged, and merging
to master does *not* force open PRs to re-run their 50-minute Windows legs.

## Close-out: v0.80.0 shipped, and both deferred items grew

v0.80.0 was tagged at `e0faf96` and released. One item: the watchdog-kill unwind (#112). The gate ran on the
**post-bump commit itself** — 14/14 — and the tag was verified by reading the peeled ref, not by trusting the
push output.

The scope decision was the day's most consequential call, and it was the operator's. Both deferred items then
justified it within hours:

- **#117 (PR 98)** reached **17 connector findings across five rounds**, and sorting them by area showed the
  pattern: argv `-m` parsing 3 rounds, capability↔redirection 2, sequence/pruning order 3, stream
  accounting/flush 3, signal-abort 2. *Every new finding was an incomplete version of the previous round's fix
  in the same area.* Each individual fix was correct and independently verified. The area was under-specified,
  so it was being discovered one instance at a time. Stopped folding and asked for five written invariants plus
  a per-invariant site enumeration — including the sites already correct, because that list is what ends the
  rounds.
- **#114 (PR 107)** turned out to need a *prerequisite*. The ancestry check accepts an empty ancestry, so a
  caller supplying the same hidden identity args that `Supervisor-IdentityArgs` emits can publish a trusted
  `startup` observation — including a startup exit 3 — with no supervisor involved. Everything hardened this
  round protects the record against **corruption** while leaving it open to **fabrication**. PR 107 is HELD on
  a design-first authorization change (#131), on the implementer's own recommendation.

The sharpest single finding of the day belongs to that second item: `test_checked_observer_accepts_identified_
console_script_ancestry` does not merely fail to catch the gap — **it asserts the permissive behaviour, and it
passes.** That is worse than absent coverage, because its existence reads as evidence the question was
considered and settled. When the fix lands, the rewrite to assert refusal must be visible in the diff rather
than a quiet deletion.

## The review-tooling experiment, scored against a real baseline

The operator asked why the automated reviewer could not be replicated in-house. Two corrections came out of it:
the reviewer is **not free** (it draws on the same Codex budget as our own workers, so it competes with them),
and the 2000-minute Actions allowance **does not bind** because this repo is public — today's burn alone would
have exceeded it many times over. The scarce resource is provider tokens, not CI minutes.

The bigger finding: **agenttalk already ships this.** `agenttalk request-launch --profile --skill --revision
--base-revision --path` queues an evidence-only ephemeral adversarial review, frozen to a full SHA, with a
fresh session and identity retired after one typed result. `ephemeral_reviewers` is `enabled:false` and
unconfigured, so it has never been used. The design already encodes lessons relearned by hand this week:
`approved` is evidence only and never a counted signoff; `needs-info`/`malformed`/no-result all fall to HOLD.

Scored two cold Claude reviewers against seven connector findings that implementers had independently
confirmed in code:

| diff | connector | cold Claude | overlap |
|---|---|---|---|
| `a51639d..13e1697` | 4 confirmed | 1 | 0 |
| `a51639d..ee177af` | 3 confirmed | 2 | 1, and more complete |

On the one shared finding the cold reviewer also named the *second* site, which the connector only found a
round later. Three new candidate defects surfaced that the connector missed, two verified real by reading the
code — including a fallback that hands a wrapper logging capability without redirection, silencing its visible
console after 256 KiB while it still reads healthy.

Verdict: **diversity, not replacement.** Run both, merge findings. And one methodological lesson that is the
lead's own: the brief demanded a constructible failure scenario and told reviewers to *drop* anything they
could not construct. Both did — one explicitly dropped the very area a connector finding covered. That traded
recall for precision, which is backwards for a merge gate. Report low-confidence, adjudicate later.

## Lead errors this session

- Called the automated reviewer "free". It is not; it shares the Codex budget.
- Recorded PR 103 as merged when it was open and red (corrected in-repo the same day, then it did merge).
- Merged the CI concurrency group describing master as "exempt from cancellation" without noting that
  `cancel-in-progress:false` inside a concurrency group means master runs **serialize**. Discovered when it
  delayed this release's own gate by ~45 minutes.
- Reported "0 of 4" and was about to report 0/7 on the review scorecard; fetching one truncated message
  changed it to 1/7 with the cold reviewer a round ahead. Truncation is not absence.
- Told the operator and a worker that a PowerShell trap was "banked" when it had only been written into a
  dispatch, not into memory.

## Resume order for whoever picks this up next

State at handoff (18:33Z, all verified, not asserted): `master = 9868532`, clean, version `0.80.0`,
tag `v0.80.0` → `e0faf96` (published release). Lead threads: `owed-inbound=0`, `reply-waiting=0`.

**v0.80.0 is done. Do not re-open it.** The two items below are v0.81.0 and neither is on a clock.

### 1. PR 98 (#117) — wrapper log capture. CLOSEST TO DONE.
Head moves often; it was `943cc28` at handoff. dev-6 owns it and is reliable — every finding it checked
this session was real, it corrected the lead's framing twice and was right both times.
- Do **not** merge on green. Green has meant nothing on this PR five times running; the connector has
  found real defects on a 15/15 head three separate times. **Read the connector on the exact head first**
  (`gh api repos/zoolok17/agenttalk/pulls/98/comments`, compare `original_commit_id` to head).
- Findings by round: 3, 2, 3, 5, 6, 2. It is converging. The argv-inference class (I2) is closed
  structurally — a declared `module_args_from` replaced grammar inference, and the prefix now rejects
  execution-mode tokens including bare positional ones.
- Outstanding when clean: one doc line naming the accepted launch-arg form, because separate-token
  `-X utf8` is now refused (attached `-Xutf8` works). Verified no shipped config or doc uses the old form.
- **When the connector is clean on the head, merge it.**

### 2. PR 107 (#114) — kill-switch startup persistence. HELD, and stay held.
Held on task #131 (launch-provenance authorization), on the implementer's own recommendation. Its red is
the `test_lanes` concurrency flake on an older head; it clears on the next push.
- The design is on master: `docs/DESIGN-supervisor-observation-authorization.md`.
- The panel result is on master: `docs/rr131-panel-disposition.md`. **Read that before touching #131.**
  4 BREAKS, 3 CANNOT-ESTABLISH, 2 HOLDS; two lenses rejected with release_blocker set.
- Do not let anyone redesign the parts that held: Q1 (core bypass premise) and Q7's closed-template method.
- Re-panel scope when the revision lands: the four BREAKS plus Q2 only.

### Lead lessons from this session, in priority order

1. **Read tool output before counting it.** Every wrong claim in the last two hours traced to piping a
   command through `grep -c` and treating the filtered result as the tool's answer. `agenttalk drain`
   was printing *"refusing an unbounded consuming read on a pipe; pass --limit N"* in plain English and
   I read it as an empty inbox — then invented a read-path defect, chased three wrong hypotheses, and
   **told the operator a healthy agent had parked.** Bounded reads, unpiped, and read the words.
2. **The lead's own instrument needs the same discipline the lead imposes.** All day I required workers to
   set `CWD` *and* `PYTHONPATH` on every call, and exempted my own commands because they were "just reads".
   Note also: the installed `agenttalk` is `0.79.1` while source is `0.80.0`. Not the cause of anything
   here, but a live skew — prefer `PYTHONPATH=<repo>/src`.
3. **Compact after a release ships.** v0.80.0 shipped at 11:48Z. That was the natural boundary; the session
   ran seven hours past it and the degradation was measurable. Do not rely on noticing — use the trigger.
4. **Sorting findings by area beats counting them.** PR 98 looked divergent at 17 findings until they were
   grouped: five areas, each repeating the previous fix. That table is what justified demanding invariants
   instead of a sixth fold, and it worked.
5. **Close reviewer threads with an outcome, same day.** Two threads were left owed for hours this session
   after the lesson had already been learned once. `owed-inbound` in `agenttalk threads` is the check.

---

## Post-compact session, 19:00Z onward — three open PRs, none of them where I thought

Resumed from the handoff at `93324c3` expecting to gate one PR and stop. Ended the stretch with three
concurrent workstreams, five new defect tasks, and two corrections to claims I had already given the
operator. Recording it because most of it is invisible in the diff.

### PR 98 (#117): the connector kept earning its place

Findings by round on this one PR: **17 -> 6 -> 2 -> 4 -> 1**. Green CI has now been wrong on it six
consecutive times. Two rounds in this stretch are worth keeping.

**The execution-mode class leaked a fourth time.** After `-P`, then `--check-hash-based-pycs always`
misparsed as a script path, then attached `-c'print(1)'`, the connector found a bare `-` — CPython's
documented stdin dispatch. It passed because it starts with `-` and matches none of the `-m`/`-c`/`--`
exclusions, so the "is this a dash-prefixed ordinary flag" predicate mistook the stdin dispatch for a
flag. With the supervisor's NUL stdin, `python - -m agenttalk ...` exits without ever running agenttalk
while the helper reports a valid launch and commits a log generation that can prune real evidence.

The instruction that mattered was *fix the enumeration, not the instance*. dev-6 extracted
`Test-AgenttalkExecutionModeToken` as the single named function that IS the terminal set, derived from
the documented grammar line, and added a test asserting 10 terminal tokens refused and 11 ordinary flags
accepted. A fifth spelling now fails a test instead of shipping.

**Then the same shape again, one layer down.** The connector found `OpenOutputOrNull` substituting `NUL`
for both stdout and stderr on a double open failure while still reporting `Redirected = $true` — so a
launch that redirected nothing commits a generation and can prune the last usable diagnostics. That is
dev-6's own I1 invariant (`9687b01`, "never commit a generation for a launch that did not actually
redirect") leaking on the path where the *substitution* succeeds so the open never fails. Fixed as a
predicate over the result — `-not (StdoutDegraded -and StderrDegraded)` — not as a special case.

dev-6 ran direction control unprompted on every fix: stash the source change, keep the new test, confirm
it FAILS, restore, confirm it passes. That is the only evidence separating a fix from a test written to
match current behaviour, and it should be the standing expectation, not a nice surprise.

### The handover question that paid for itself

codex-2 had been parked 20h on `worktree_branch_already_checked_out`. The message telling it to reuse
the existing worktree **queued behind the parked message** — the fix was unreachable because the agent
was blocked. That is #122 with a live field instance.

Before cleaning anything up I asked dev-6 whether its branch actually covered codex-2's work. It did
not. Two finished, tested, **uncommitted** pieces were sitting in `.worktrees/codex2-task117`: native-fd
sealing for the log bound (`dup2` the inherited fd to devnull, segments pre-opened `O_CREAT|O_EXCL`), and
entry-point capture in `__main__.py` so a pre-argparse exception cannot write an unbounded traceback.
Five passing tests already existed for them. Plus ~3232 insertions of process-tree-kill hardening that
is not #117's scope at all. All of it one `git worktree prune` from gone, on a host carrying 117
unreaped worktrees (#97). Filed as #135, #136, #137 with the test names recorded so nobody re-derives
them.

The native-fd gap was then **deferred on evidence rather than vibes**: dev-6 read every `subprocess`
call site individually, checked `os.write`, `sys.__stdout__`, the `_ProcStream` path and
`dependencies = []`, and established the population of native fd writers is empty today. It also checked
whether we were overclaiming and found the tutorial had stated the limitation since #117's first commit
— then moved that caveat into `wrapper_logs.py`'s own docstring so it cannot drift from the tutorial.

### #120 was marked completed and is not merged

dev-4 refused to have its #87-A design panelled because the design cited a #120 closure API that does
not exist. Verified: `28f663f` is not an ancestor of master, PR 102 has been open since 2026-07-30, and
it had **zero** connector comments in 25 hours. My task list said #120 was done, so the design was built
on a premise my own bookkeeping supplied. The real #120 delivers a 64-entry snapshot and a deny-only
post-kill barrier, and explicitly lets late descendants survive the planned Stop-Tree.

Requesting the missing review returned four findings immediately, one of them destructive: the tree
establishes identity with an exact FILETIME and then **drops it** when building the kill target, so
`Stop-Tree` guards only on a rounded timestamp string and a recycled PID that renders the same gets
killed instead — while the code holds the value that would have distinguished it. A second finding is
the same recycled-PID root cause failing the opposite way (unrelated processes seed the barrier and turn
a complete tree into a sticky HOLD). PR 102 is also `DIRTY` against master.

### CodeQL is a reporter, not a gate

I told the operator PR 102's red was probably a GitHub default-setup collision. **Wrong** — default
setup is `not-configured`, and the 3-second `CodeQL` check is the results reporter doing its job. What
it led to matters more than PR 102: two high-severity alerts have been open on **master** since
2026-07-05 and 2026-07-21, across multiple releases, while the ruleset-required
`codeql (security-extended)` passes anyway. Green has meant "the analyzer completed", not "no open
highs". Filed as #138 with both dispositions determined by reading the code: `cli.py:8620` is a false
positive (the env read feeds a boolean, the taint is broken by the coercion), `_atomic.py:100` is a true
positive by design (every state write funnels through it, including the HMAC secret) that needs an
explicit threat-model dismissal rather than silence.

### Operator decision recorded: #87 M5 = Option A

The no-new-plane promise is **absolute and exception-free** — no daemon, no persistence plane, no durable
OS object, no runtime dependency, and no separately versioned exception. Where action-scoped
child-creation closure cannot be proven the answer is `CAPABILITY_UNAVAILABLE`, no closure-dependent
teardown proceeds, and recovery stays `POLICY_HELD` pending a human. The operator chose that knowing it
means some recoveries never become automatic, preferring it to stale-authority risk. Folded at
`85e4bc0`. **Do not reintroduce a mechanism later as an implementation detail.**

### Lessons this stretch adds to the five above

6. **A per-agent WARN can be a queue, not a fact.** `status` showed dev-4 owing one escalation; it owed
   five. Resolving the first revealed the second. The only way to see the whole backlog was
   `threads --for <agent>`. Filed as #134 — a warning that is numerically wrong is worse than a missing
   one, because it reads as complete.
7. **Verify the explanation, not just the symptom.** I diagnosed the CodeQL red from its shape (3 seconds,
   therefore infrastructure) and reported that before checking `code-scanning/default-setup`. The check
   was telling the truth the whole time.
8. **Ask the outgoing owner before reaping anything.** "dev-6's branch is a strict superset" was true by
   line count and false in substance. One question preserved five passing tests and three tasks' worth of
   work.
9. **Sequence a panel behind a base that is mid-change.** #87-A is panel-ready at `85e4bc0` but two of the
   PR 102 findings rewrite the identity semantics its authority argument reasons about. Applying dev-4's
   own standard back to it: do not review against a moving foundation.
